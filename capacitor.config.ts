import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rptaskmanagement.com',
  appName: 'rptaskmanagement',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
